#/bin/sh

# this script is to build gcc with coverage option
echo "Qiskit coverage is actually already build within the original docker image."
echo "This script is a placeholder, feel free to run 'python tools/coverage/QISKIT/collect_coverage.py --folder {folder}' to collect coverage"
