#/bin/sh

# this script is to build gcc with coverage option
echo "For javac coverage, we directly use the Hephaestus docker image which contains pre-build javac with coverage option."
echo "We also include the full script we use to collect the java coverage in the 'tools/JAVA' folder"
